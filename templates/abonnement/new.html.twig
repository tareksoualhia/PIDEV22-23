{% extends 'base.html.twig' %}

{% block body %}
    <h1>New Abonnement</h1>

    {{ form_start(form) }}
        {{ form_row(form.type) }}
        {{ form_rest(form) }}
        <button type="submit">Create</button>
    {{ form_end(form) }}
{% endblock %}