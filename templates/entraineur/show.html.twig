{% extends 'base.html.twig' %}

{% block title %}Entraineur{% endblock %}

{% block body %}
    <h1>Entraineur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>HoraireDisponibilite</th>
                <td>{{ entraineur.horaireDisponibilite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_entraineur_index') }}">back to list</a>

    <a href="{{ path('app_entraineur_edit', {'ld': entraineur.ld}) }}">edit</a>

    {{ include('entraineur/_delete_form.html.twig') }}
{% endblock %}
